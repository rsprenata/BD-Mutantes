<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rsprenata.bd_mutantes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Dashboard" />
        <activity
            android:name=".SplashScreen"
            android:label="Splash Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroActivity"
            android:label="Cadastro" />
        <activity
            android:name=".FormHabilidadeActivity"
            android:label="Adicionar Habilidade" />
        <activity
            android:name=".ListarActivity"
            android:label="Listar Mutantes" />
        <activity
            android:name=".DetailActivity"
            android:label="Detalhes Mutante" />
        <activity
            android:name=".FormMutanteActivity"
            android:label="Editar Mutante" />
        <activity android:name=".PesquisarActivity"></activity>
    </application>

</manifest>